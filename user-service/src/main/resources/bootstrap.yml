spring:
  application:
    name: userservice
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      server-addr: 43.142.80.191:8848 # nacos地址
      discovery:
        cluster-name: hz
        namespace: dev
      config:
        file-extension: yaml # 文件后缀名